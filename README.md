# Laravel Blog Application

โปรเจคนี้เป็นแอปพลิเคชันบล็อกง่ายๆ ที่พัฒนาด้วย Laravel 10 เหมาะสำหรับผู้ที่กำลังเรียนรู้ Laravel และต้องการศึกษาการทำงานของระบบพื้นฐาน เช่น CRUD Operations, Database Migration, และ Routing

## 📋 คุณสมบัติหลัก

- ✅ จัดการบทความ (Create, Read, Update, Delete)
- ✅ ระบบ Pagination สำหรับแสดงรายการบทความ
- ✅ การตรวจสอบข้อมูล (Form Validation)
- ✅ ใช้งาน Database ผ่าน Query Builder
- ✅ Routing แบบ Resourceful

## 🛠️ ข้อกำหนดเบื้องต้น

ก่อนเริ่มต้น ตรวจสอบให้แน่ใจว่าคุณมี:

- PHP >= 8.1
- Composer
- MySQL หรือ MariaDB
- Web Server (Apache, Nginx หรือ PHP Built-in Server)

## 🚀 การติดตั้งและตั้งค่า

### 1. Clone โปรเจค
```bash
git clone <repository-url>
cd basic-laravel10-fullCourse
```

### 2. ติดตั้ง Dependencies
```bash
composer install
npm install
npm run build
```

### 3. ตั้งค่าไฟล์ Environment

คัดลอกไฟล์ `.env.example` เป็น `.env`:
```bash
cp .env.example .env
```

สร้าง Application Key:
```bash
php artisan key:generate
```

### 4. การตั้งค่าฐานข้อมูล

แก้ไขไฟล์ `.env` สำหรับเชื่อมต่อฐานข้อมูล:

```env
# ข้อมูลการเชื่อมต่อฐานข้อมูล
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=blogdb          # เปลี่ยนเป็นชื่อฐานข้อมูลของคุณ
DB_USERNAME=root            # เปลี่ยนเป็น username ของคุณ
DB_PASSWORD=                # เปลี่ยนเป็น password ของคุณ

# ข้อมูลพื้นฐานของแอปพลิเคชัน
APP_NAME=Laravel Blog
APP_ENV=local
APP_DEBUG=true
APP_URL=http://localhost
```

**⚠️ ข้อควรทราบ:** อย่าลืมสร้างฐานข้อมูลชื่อ `blogdb` ใน MySQL ก่อนรัน migration

### 5. Migration ฐานข้อมูล

รันคำสั่งต่อไปนี้เพื่อสร้างตารางในฐานข้อมูล:

```bash
# สร้างตารางทั้งหมดตาม migration files
php artisan migrate
```

ตารางที่ถูกสร้าง:
- `users` - ตารางผู้ใช้ (มาพร้อมกับ Laravel)
- `password_reset_tokens` - ตารางรีเซ็ตรหัสผ่าน
- `failed_jobs` - ตารางงานที่ล้มเหลว
- `personal_access_tokens` - ตาราง token สำหรับ API
- `blogs` - ตารางบทความ (สร้างโดยโปรเจคนี้)

### 6. เริ่มต้นเซิร์ฟเวอร์

```bash
php artisan serve
```

เข้าชมแอปพลิเคชันที่: `http://localhost:8000`

## 📁 โครงสร้างไฟล์ที่สำคัญ

```
app/
├── Http/Controllers/
│   └── AdminController.php      # Controller หลักสำหรับจัดการบล็อก
├── Models/
│   └── Blog.php                 # Model สำหรับบทความ
database/
├── migrations/
│   └── 2025_11_15_092735_create_blogs_table.php  # Migration สำหรับตาราง blogs
resources/views/
├── layout.blade.php             # Template หลัก
├── welcome.blade.php            # หน้าแรก
└── client/
    ├── about.blade.php          # หน้า About
    ├── blog.blade.php           # หน้ารายการบทความ
    ├── form.blade.php           # ฟอร์มสร้าง/แก้ไขบทความ
    └── edit.blade.php           # หน้าแก้ไขบทความ
routes/
└── web.php                      # Route definitions
```

## 🛣️ Routes ที่มีให้

| Method | URI | ชื่อ Route | คำอธิบาย |
|--------|-----|-------------|-----------|
| GET | `/` | - | หน้าแรก (Welcome) |
| GET | `/about` | `about` | หน้า About |
| GET | `/blog` | `blog` | แสดงรายการบทความ (พร้อม pagination) |
| GET | `/create` | `create` | ฟอร์มสร้างบทความใหม่ |
| POST | `/insert` | `insert` | บันทึกบทความใหม่ |
| GET | `/edit/{id}` | `edit` | ฟอร์มแก้ไขบทความ |
| POST | `/update/{id}` | `update` | อัพเดทบทความ |
| GET | `/delete/{id}` | `delete` | ลบบทความ |

## 💾 โครงสร้างตาราง Blogs

```sql
CREATE TABLE blogs (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    title VARCHAR(255) NOT NULL,
    content TEXT NOT NULL,
    status ENUM('draft', 'published') DEFAULT 'draft',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

## 🎯 ฟังก์ชันการทำงานหลัก

### 1. แสดงรายการบทความ (`/blog`)
- แสดงบทความทั้งหมดแบบแบ่งหน้า (5 รายการต่อหน้า)
- ใช้ `DB::table('blogs')->paginate(5)`

### 2. สร้างบทความใหม่ (`/create` → `/insert`)
- แสดงฟอร์มสำหรับกรอกข้อมูล
- ตรวจสอบข้อมูล (Validation):
  - `title`: ต้องกรอก, อย่างน้อย 5 ตัวอักษร
  - `content`: ต้องกรอก, อย่างน้อย 10 ตัวอักษร

### 3. แก้ไขบทความ (`/edit/{id}` → `/update/{id}`)
- ดึงข้อมูลบทความเดิมมาแสดงในฟอร์ม
- ตรวจสอบข้อมูลเหมือนการสร้างใหม่
- อัพเดทเฉพาะข้อมูลที่เปลี่ยนแปลง

### 4. ลบบทความ (`/delete/{id}`)
- ลบบทความตาม ID ที่ระบุ
- ไม่มีการยืนยันการลบ (ในโปรเจคจริงควรเพิ่ม)

## 🔧 การปรับแต่งเพิ่มเติม

### การเพิ่มข้อมูลตัวอย่าง

สร้าง Seeder เพื่อเพิ่มข้อมูลตัวอย่าง:

```bash
php artisan make:seeder BlogSeeder
```

แก้ไขไฟล์ `database/seeders/BlogSeeder.php`:

```php
public function run()
{
    DB::table('blogs')->insert([
        [
            'title' => 'บทความแรกของฉัน',
            'content' => 'นี่คือเนื้อหาของบทความแรก...',
            'status' => 'published',
            'created_at' => now(),
            'updated_at' => now(),
        ],
        // เพิ่มข้อมูลเพิ่มเติม...
    ]);
}
```

รัน Seeder:
```bash
php artisan db:seed --class=BlogSeeder
```

### การ Reset ฐานข้อมูล

หากต้องการล้างข้อมูลและเริ่มใหม่:
```bash
php artisan migrate:fresh --seed
```

## 🐛 การแก้ไขปัญหาที่พบบ่อย

### 1. ข้อผิดพลาดการเชื่อมต่อฐานข้อมูล
```
SQLSTATE[HY000] [1045] Access denied for user
```
**วิธีแก้:** ตรวจสอบการตั้งค่าใน `.env` ให้ถูกต้อง

### 2. ไม่พบฐานข้อมูล
```
SQLSTATE[HY000] [1049] Unknown database 'blogdb'
```
**วิธีแก้:** สร้างฐานข้อมูล `blogdb` ใน MySQL ก่อน

### 3. ข้อผิดพลาดการ Migration
```
SQLSTATE[42S01]: Base table or view already exists
```
**วิธีแก้:** รัน `php artisan migrate:fresh` เพื่อเริ่มใหม่

## 📚 แหล่งเรียนรู้เพิ่มเติม

- [Laravel Documentation (Thai)](https://laravel.com/docs)
- [Laravel Bootcamp](https://bootcamp.laravel.com)
- [Laracasts](https://laracasts.com)

## 👨‍💻 สำหรับผู้เริ่มต้น

ถ้าคุณกำลังเรียน Laravel โปรเจคนี้เหมาะสำหรับการศึกษา:

1. **Routing** - ดูที่ `routes/web.php`
2. **Controller** - ศึกษา `app/Http/Controllers/AdminController.php`
3. **Database** - ดู `database/migrations/` และการใช้ Query Builder
4. **Views** - ศึกษาไฟล์ใน `resources/views/`
5. **Form Validation** - ดูการใช้ `$request->validate()` ใน Controller

## 📝 หมายเหตุ

- โปรเจคนี้ใช้ Query Builder ไม่ใช่ Eloquent ORM เพื่อความเรียบง่ายสำหรับผู้เริ่มต้น
- ยังไม่มีระบบ Authentication (เพิ่มในภายหลังได้)
- ใช้ Bootstrap สำหรับ styling (ถ้ามี)
- เหมาะสำหรับการเรียนรู้พื้นฐาน Laravel CRUD

---

**Happy Coding! 🎉**